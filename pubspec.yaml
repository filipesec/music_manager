name: music_manager
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.0

dependencies:
  dotted_border: ^3.1.0
  flutter_bloc: ^9.1.1
  equatable: ^2.0.8
  drift: ^2.34.0
  sqlite3: ^3.3.3
  path_provider: ^2.1.5
  path: ^1.9.1
  file_picker: ^10.3.10
  image_picker: ^1.2.2
  just_audio: ^0.10.5
  
  flutter:
    sdk: flutter
  drift_flutter: ^0.3.0

dev_dependencies:
  drift_dev: ^2.34.0
  build_runner: ^2.15.0
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/